# Variável e Tipo de dados

Neste trecho de material contém os primeiros passos e conceitos fundamentais de programação explorados em diferentes linguagens, como JavaScript, PHP e Python.

### Variáveis:
É um espaço na memória do computador reservado para armazenar dados que podem ser modificados/alterados conforme a execução do código/programa.

### Tipos de Dados:
Toda linguagem de programação utiliza tipos de dados para processar as insformações, sendo as princpais abordadas neste trecho:
    
* **String =>** Textos ou caracteres delimitados por aspas.(ex.: "Olá Mundo!").
* **Int/Float =>** tipo de dado que trata exclusivamente de números, sendo Int os números inteiros(5,10,100) e o Float números decimais (8.5 | 0.6).
* **Boolean =>** Valores lógicos , sendo eles apenas Verdadeiro (true) ou Falso(false). A considerar que segue a lógica binária (true = 1) e (false=0).

**CONSTANTE:**
Assim como uma variável ela armazena um dado, porém com uma particularidade, este valor não pode ser alterado.

---
#### Arquivos na pasta:
* *variaveis.js* => exemplos de declaração e tipos de dados, com particularidades do JavaScript(let, const).
* *variaveis.php* => exemplos de declaração e tipos de dados, com particularidades do PHP ($, define).
* *variaveis.py* => exemplos de declaração e tipos de dados, com particularidades do Python (ausência do ponto e vírgula (;), não é necessário um símbolo ou função para declarar variáveis, sendo uma linguagem dinâmica e simples).
